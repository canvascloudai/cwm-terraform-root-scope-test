# cwm-terraform-root-scope-test

Safe, non-deployed fixture for testing Cloud World Model Terraform root-scope and multi-directory behavior.

This repository is not applied and does not include credentials. Do not run `terraform apply`.
